export default function Route({ path, Component }) {
  // This component return nulls because all the work of routing is performed by
  // 'Router' Component.
  // The only thing useful from this components are his props, they are passed
  // to the Router Component, as child.
  return null;
}
